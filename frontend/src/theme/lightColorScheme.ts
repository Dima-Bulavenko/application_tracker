import { ColorSystemOptions } from '@mui/material';

// https://mui.com/material-ui/customization/palette/#color-schemes
export const Palette: ColorSystemOptions['palette'] = {
  // write custom colors here
};

export const ColorScheme: ColorSystemOptions = {
  palette: Palette,
};
